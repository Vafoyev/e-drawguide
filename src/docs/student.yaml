paths:
  /student/library:
    get:
      tags: [Student]
      summary: Kutubxona ro'yxati
      security:
        - bearerAuth: []
      parameters:
        - name: lang
          in: query
          schema: { type: string, enum: [uz, ru, en] }
        - name: search
          in: query
          schema: { type: string }
        - name: page
          in: query
          schema: { type: integer, default: 1 }
      responses:
        200:
          description: OK
  /student/videos:
    get:
      tags: [Student]
      summary: Video darslar ro'yxati
      security:
        - bearerAuth: []
      parameters:
        - name: search
          in: query
          schema: { type: string }
        - name: page
          in: query
          schema: { type: integer, default: 1 }
      responses:
        200:
          description: OK
  /student/quizzes:
    get:
      tags: [Student]
      summary: Faol testlar ro'yxati
      security:
        - bearerAuth: []
      responses:
        200:
          description: OK
  /student/quizzes/{id}:
    get:
      tags: [Student]
      summary: Test savollarini olish
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        200:
          description: OK
  /student/quizzes/{id}/submit:
    post:
      tags: [Student]
      summary: Test natijasini topshirish
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required: [answers]
              properties:
                answers:
                  type: array
                  items:
                    type: object
                    required: [question_id, selected_option]
                    properties:
                      question_id: { type: string, format: uuid }
                      selected_option: { type: string }
      responses:
        200:
          description: OK